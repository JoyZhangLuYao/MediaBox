<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!--<style name="MyBar" parent="@android:style/Widget.SeekBar">
        <item name="indeterminateOnly">false</item>
        <item name="progressDrawable">@drawable/bg_bar</item>
        <item name="indeterminateDrawable">@null</item>
        <item name="minHeight">5dip</item>
        <item name="maxHeight">5dip</item>
        <item name="thumb">@mipmap/vol_c</item>
        <item name="thumbOffset">1dip</item>
        <item name="focusable">true</item>
        <item name="mirrorForRtl">true</item>


    </style>-->
    <style name="AnimationPreview">
        <item name="android:windowEnterAnimation">@animator/push_in</item>
        <item name="android:windowExitAnimation">@animator/push_out</item>
    </style>

</resources>
